// One easily-editable constant for the support contact address, used by both
// the mailto: account-deletion flow and its plain-text visible fallback -
// never hardcode this email at an individual call site.
export const SUPPORT_EMAIL = 'hiorivori@gmail.com';
