# Bhav Bhakti — Project Context

*Last verified against actual code/config: 2026-08-13 (see §45 — Firebase Phone Auth Phase 5 shipped: the fake OTPless bypass (master OTP `123456`, `TEST_NUMBERS`, `SEND_OTP_ENABLED`) is fully REMOVED, not just disabled, closing the real security hole this app carried since the start; old `/send-otp`/`/verify-otp`/`/resend-otp` routes and `otpless.service.js` deleted entirely; ALL 5 Firebase Phone Auth phases now DONE/COMMITTED/TESTED ON-DEVICE; real-number sign-in still on the reCAPTCHA fallback pending real Play Store distribution (see §43), SMS auto-fill deliberately ruled out (Play Store rejection risk). §43 — Firebase Phone Auth Phases 1-4 built and committed, working end-to-end with test phone numbers; real phone numbers now RESOLVED as of 2026-08-13 — confirmed working via Firebase's own auto-provisioned reCAPTCHA v2 fallback (the earlier "set up reCAPTCHA" Console message was a red herring for reCAPTCHA Enterprise, an unrelated optional product); Play Integrity itself remains unavailable for USB-sideloaded dev builds by Google's own design (not fixable via config), with the real fix — Play Console internal testing distribution — deliberately deferred until closer to real Play Store release. §44 — routine dependency-drift cleanup and an app icon resize, both committed and on-device verified. §42 — Rashifal Phase 4 shipped (paywall gate on the 12-sign grid, date-nav pill removed from `horoscope-detail.tsx`) and the scattered `isPremiumUser` stubs consolidated onto a single shared `usePremiumStore`, both DONE/COMMITTED/TESTED ON-DEVICE — all 5 Rashifal phases now complete. §40 — Rashifal MVP core built, DONE/COMMITTED/TESTED ON-DEVICE: real Gemini-generated daily horoscope content, dead reactive fallback chain removed, Home's "Today's Horoscope" card now collects a birthdate once via a native picker and routes straight to the user's own sign, never paywalled (separate 12-sign grid unchanged, paywall still Phase 4); three real bugs found in previously-untested code and fixed — a profile API URL that never existed, 8 horoscope routes hung forever from an uninvoked `validate` reference, and a significant UTC/local timezone bug silently serving IST users yesterday's horoscope every night between midnight-5:30am across 6 live call sites, now fixed via one shared local-date utility mirrored in both repos; Phase 4 paywall and Phase 5 final verification still pending). §41 — trending's 7-day expiration window removed from `getTrendingFeeds` (now matches mantra's existing no-window behavior), committed `a4d16e5`, previously undocumented. §39 — app branding (icon, splash screen, display name, package identifier) DONE and committed, `c9d4632`/`c9071b8`; real gotcha was Android's adaptive-icon shape-masking cropping edge-to-edge artwork, needed real margin/padding in the source canvas, took several iterations to get right; old dev-client builds under the previous package name need manual uninstall on test devices, won't auto-update). §38 — lock-screen metadata mismatch fix (§37's finding) built and committed, PARTIAL: title fix confirmed working via real-device testing, though not yet proven against a file with genuinely competing embedded title data; artwork fix confirmed present/compiled but NOT effective, since it disables the wrong mechanism against embedded ID3 cover art; committed as-is, honestly documented rather than presented as done; next real test is uploading a genuine thumbnail to see which source wins). §37 — lock-screen/notification skip-vanish bug: two real, distinct root causes found and fixed, tested on-device, notification shade confirmed seamless on skip; lock screen improved but still has rough edges, deliberately not polished further yet; a new, separate metadata race finding logged — the audio file's own embedded metadata overwrites the app's correct data about a second after it first displays correctly (now partially addressed, see §38). §36 — skip-button fix DONE, committed, tested on a real device on both the notification shade and lock screen; the buildFromSource root cause is confirmed via dex decompilation, not just a successful build, and the connection-pool fix from weeks ago is re-confirmed genuinely working for the first time with real evidence). §35 — lock-screen skip-button fix showed zero on-device effect; root cause found via dex-file decompilation of the actual installed APK: `expo-audio` links a stale prebuilt AAR instead of compiling from the patched source; a config fix was applied to `package.json` but is NOT yet rebuilt/verified; the earlier connection-pool fix's "confirmed on device" status is now in doubt too, pending re-verification). §34 — Real content sharing built via `react-native-share`, replacing React Native's built-in text-only Share; committed in `bhav-bhakti-fe` through `0355978`/`51a6a0d`; wallpaper/video confirmed on-device, audio branch built but untested — blocked on no real thumbnail content existing yet, not a code gap). §33 — Wallpaper Hub video support + Viewing Window feature shipped, committed and pushed in `bhav-bhakti-fe` through `65be4d7`; three real bugs found and fixed during testing; the `react-native-pager-view` Reels-style rebuild scoped in §32 is now abandoned, not just deferred — the founder made a deliberate call to go with the auto-looping video grid instead). §32 — Status sub-tab Reels-style feed's first attempt, a FlatList-based snap-scroll, was built then fully reverted this session and never committed; two real bugs found/fixed along the way are kept there for reference, not the revert reason; a `react-native-pager-view` rebuild is scoped with a full phased plan ready for next session; `bhav-bhakti-be` untouched). §31 — Home feed autoplay's Phase 5 confirmed already built alongside Phase 4 rather than separately pending, a real app-backgrounding autoplay-stop bug fixed and pushed in `bhav-bhakti-fe` through `a21460b`, and a Wallpaper Hub snap-to-card feed scoped but not built (`bhav-bhakti-be` untouched that session). §30's Phases 1-4 remain committed and pushed in `bhav-bhakti-fe` through `39e5fc9` (`bhav-bhakti-be` untouched that session). §25 and §26's code work is committed and pushed in both repos — `bhav-bhakti-fe` through `4389e55`, `bhav-bhakti-be` through `4d6701b` — see §26's closing note for the full commit breakdown. §27's work is committed and pushed in `bhav-bhakti-fe` through `8d0b227` (`bhav-bhakti-be` untouched this session). §28's work is committed and pushed in `bhav-bhakti-fe` through `2f4afdf` (`bhav-bhakti-be` untouched). §29 was investigation/scoping only, no code written. The two `Content Upload Sheet` CSV files in `bhav-bhakti-be` remain a deliberate exception, left uncommitted — see §26. **On 2026-07-29, this file was condensed for size** (it had exceeded Claude Code's ~150k-char limit): §11–21 (closed, superseded sessions) were compressed into short summaries, §22–27 were moderately tightened while keeping their still-load-bearing architecture/decisions intact, and §1–10/§6/§7 were left at or near full detail since they describe still-open gaps or are already historical. If older blow-by-blow detail is ever needed, check `bhav-bhakti-fe`/`bhav-bhakti-be`'s git history for this file.*

## 1. Project overview

Bhav Bhakti is a devotional content app: wallpapers, ringtones, daily horoscopes (Rashifal), quizzes, and a social "feed" of devotional content (mantras, etc.).

Two sibling repos live under this parent folder (`D:\bhav_bhakti`, which is **not itself a git repo** — only the two subfolders below are):
- `bhav-bhakti-fe` — Expo / React Native frontend (Expo Router, TypeScript, Zustand, `expo-av`). GitHub: `orivori/bhav-bhakti-fe`.
- `bhav-bhakti-be` — Express / Sequelize backend (MySQL). GitHub: `orivori/bhav-bhakti-be`.

**Founder context:** solo, non-technical founder. No dev team — all implementation work goes through Claude Code. When a decision has technical tradeoffs, explain them in plain language before the founder has to choose.

## 2. Backend infrastructure

**Database:** MySQL, run natively on local Windows for development (`DB_HOST=localhost`, `.env` in `bhav-bhakti-be`). Not Docker for local dev — `dev`/`start` npm scripts run plain `nodemon`/`node` against local MySQL directly. Note: Docker files (`Dockerfile`, `docker-compose.yml`, `docker-compose.cost-efficient.yml`) still exist in the repo and are referenced by `DEPLOYMENT_GUIDE.md` as a planned AWS EC2 + Docker **production** deployment path — they weren't deleted, just unused for local dev. Don't assume "Docker was abandoned" means the files are gone.
- **20** Sequelize models in `src/models/` (Category, DailyHoroscope, Deity, Feed, FeedDownload, FeedLike, FeedMedia, FeedShare, FeedTag, HoroscopeFeedback, HoroscopeReadHistory, LoginHistory, OTPLog, Quiz, QuizOption, QuizQuestion, QuizResponse, User, UserProfile, UserZodiac).
- **35** migration files in `src/database/migrations/`.
- **No external/cloud DB hosting yet** — confirmed no Railway config, no `app.yaml`, no Procfile, no active Cloud SQL setup. This remains a real gap before launch. (Railway vs. Google Cloud SQL was discussed, Railway favored for lower setup complexity — no infra has been provisioned yet either way.)

**Media storage:** fully migrated from AWS S3 to Firebase Cloud Storage.
- `src/services/uploadService.js` was rewritten around a custom multer storage engine (`FirebaseStorageEngine`) that streams uploads to Firebase via `firebase-admin`; Sharp-based image optimization logic was preserved.
- Bucket: `bhav-bhakti.firebasestorage.app` (`src/config/firebase.config.js`, `.env.example`). Region/location (asia-south1/Mumbai) is a Firebase Console project setting, not stored anywhere in the codebase, so it couldn't be verified from code — treat that detail as unconfirmed until checked in the console. (Likely conflated with the old S3 bucket, which *was* explicitly `ap-south-1`/Mumbai per `S3_SETUP_INSTRUCTIONS.md`.)
- `storage.rules` is deployed exactly as intended — public read, no client writes:
  ```
  allow read: if true;
  allow write: if false;
  ```
  Writes only ever happen server-side via the Admin SDK, which bypasses these rules.
- Service account key: `src/config/firebase-service-account.json` — confirmed present on disk, confirmed listed in `bhav-bhakti-be/.gitignore` (line 6), confirmed **not tracked** by git (`git ls-files` has no entry for it). Safe as of this writing — but re-check this before every commit, since a gitignore only prevents *future* accidental adds, not a `git add -f` or a rule that gets edited out later.
- **Confirmed working end-to-end (2026-07-18):** this is no longer just a migrated code path — it was exercised for real. A real audio file was uploaded through the live `POST /api/v1/upload/media` endpoint (not a direct Firebase Admin SDK call), the returned Firebase URL was used to create a Feed via the live `POST /api/v1/feed` endpoint (Feed ID `1`, `type: 'ringtone'`), and the ringtone played back correctly in the app's Ringtones tab. Full path verified: upload → Feed/FeedMedia rows → app playback.

**Auth:** confirmed broken/placeholder, not yet replaced.
- OTPless is wired up in `src/services/otpless.service.js` (real API calls to `auth.otpless.app`), but it's fully bypassed in practice: `SEND_OTP_ENABLED = false` in `src/constant/index.js` short-circuits sending, and `src/facades/auth.facade.js` accepts a hardcoded master OTP (`'123456'`) for **any** phone number, plus a `TEST_NUMBERS` allowlist. Nothing about this is production-safe.
- Firebase Auth (phone OTP + Google login) was the identified replacement direction, but **there is no implementation of it anywhere** — `firebase-admin` is only used for Storage (`getStorage(app)`), never `getAuth`; no `firebase-admin/auth` import exists in the backend; the frontend has no Firebase SDK or Google-login code at all, only calls to the backend's existing phone-OTP endpoints. This is 100% unbuilt, not partially built.

**AWS leftovers not yet cleaned up** (low priority — flag if touching these areas, don't proactively fix):
- `aws-sdk`, `@aws-sdk/client-s3`, and `multer-s3` are still in `bhav-bhakti-be/package.json` dependencies though unused by the live Firebase upload path.
- `scripts/optimize-s3-assets.js` (`npm run optimize:s3`) is a standalone AWS-SDK script, now orphaned since new uploads no longer land in S3.
- `S3_SETUP_INSTRUCTIONS.md` and `scripts/S3_OPTIMIZATION_GUIDE.md` are stale docs from the S3 era.
- `src/controllers/profile.controller.js:518` still branches on URL host string to decide whether to attempt storage cleanup on profile-picture delete:
  ```js
  if (profilePictureUrl.includes('firebasestorage.googleapis.com') || profilePictureUrl.includes('amazonaws.com') || profilePictureUrl.includes('assets.orivori.com')) {
  ```
  Legacy S3/CDN URLs will silently no-op here since `extractKeyFromUrl()` only correctly parses the Firebase key shape.

**Legacy data trap:** some `FeedMedia.mediaUrl` rows imported from old CSVs are still `cloudfront.net` URLs, sitting in the same table as new `firebasestorage.googleapis.com` rows. Harmless for rendering today (the frontend treats every media URL as an opaque string), but don't write future backend logic that assumes every stored URL is Firebase-shaped.

## 3. Content ingestion — known gap

**There is no admin panel, dashboard, or CMS anywhere in either repo.** Confirmed by direct file search — nothing named admin/CMS/dashboard exists in the frontend at all.

Current real-world workflow for adding content is one of:
1. Manual API calls (Postman/curl): `POST /api/v1/upload/media` to get a Firebase URL back, then `POST /api/v1/feed` with that URL in a `media[]` array to create the `Feed`/`FeedMedia` rows.
2. `npm run import:feeds` (`scripts/import-feeds-from-csv.js`) — reads a CSV (exported from a Google Sheet) and inserts `Feed`/`FeedMedia` rows directly via Sequelize, bypassing the API. The CSV itself still instructs the human filling it out to "upload on s3, take url from cloudflare and put here" — i.e. the sheet-based process predates the Firebase migration and hasn't been updated.

**Security note, not urgent right now:** `POST /api/v1/feed` (`src/routes/feed.routes.js:16-23`) has its auth check commented out — `// authenticate, // Commented out for public access` — with a comment saying it was made public "for admin web interface." That web interface was never built, so right now this content-creation endpoint is open to anyone who finds it. Flag as a pre-launch fix, not something to silently patch without asking (removing it will break the current manual-curl workflow until a real admin auth story exists).

**Priority:** a simple, non-technical-friendly content upload tool is a near-term goal. Don't assume this has been built in a future session without checking — confirm with the founder first.

**Content management decision — three options being weighed, not yet decided (2026-07-19), pending a discussion with the co-founder.** This supersedes the single "agreed plan" from 2026-07-18, which is now folded in as option (b):
- **(a) Adapt the existing CSV importer for Firebase URLs** — cheapest, ~1-2 hours. `scripts/import-feeds-from-csv.js` already reads `title_hi`/`caption_hi` columns correctly (see §9) and just needs its expected URL format updated for Firebase instead of the old S3/CloudFront convention. Matches the co-founder's established bulk-upload habit from the AWS era. Downside: no editing or deleting after import — mistakes require a direct DB fix.
- **(b) Build a minimal custom admin upload tool** — ~3-5 hours. The original 2026-07-18 plan: a single-page local-only form (file picker, type dropdown, category dropdown populated from `GET /categories?type=`, a title field) that calls the existing `POST /upload/media` then `POST /feed` endpoints in sequence — no new backend functionality needed. Form-based, one item at a time, but includes basic edit/delete unlike (a). The one backend change this needs: gate `/upload/media` and `/feed` behind a simple shared-secret middleware (header/query param checked against an `ADMIN_KEY` env var) — **not** real user auth, since `authenticate` runs through the broken OTPless flow (see §2) and would buy nothing.
- **(c) Adopt Directus as a full CMS** — ~1-2 days. Confirmed compatible with the existing MySQL schema via a full audit: real FK constraints already exist (`Feed→Category`, `Feed→Deity`, `FeedMedia→Feed`, quiz nesting), so Directus can sit on top with no restructuring needed. Offers native bulk CSV import *and* editing/deletion *and* multi-user roles — the most capable option. Costs the most setup time (multilingual JSON fields need per-column interface configuration; role/permission setup to hide PII/analytics tables takes real effort), and requires an ongoing schema-change discipline: structural changes only ever happen via Sequelize migrations in `bhav-bhakti-be`, never through Directus's own Data Model UI, or the ORM and the live DB schema will drift apart.
- **Wallpapers tab fix is blocked regardless of which option is picked** — see §10. Fixing its data source is pointless until a reachability decision is also made (new tab slot vs. a link from Home), so don't treat shipping any of (a)/(b)/(c) as automatically unblocking that fix.

## 4. Frontend display — known gap, re-verify before assuming fixed

**Ringtones: fully wired, working.** `app/(main)/ringtones.tsx` → `RingtonesScreen` → `useRingtones` hook → `feedService.getFeeds()` (filtered client-side for `type === 'ringtone'`) → rendered by `RingtoneFeedCard`, which plays audio via `expo-av`'s `Audio.Sound.createAsync({ uri: audioMedia.audioUrl || audioMedia.mediaUrl })`. This is the one real, testable end-to-end content path in the app today.

**Wallpapers: still broken as of this writing, and now confirmed unreachable too.** The actual "Wallpapers" screen (`app/(main)/wallpapers.tsx`, and `wallpaper-detail.tsx`) imports and renders `mockWallpapers` from `src/data/mockWallpapers.ts` — static hardcoded data, never calls the Feed API. Anything added through the ingestion paths in §3 will **not** appear here. **On top of that, a 2026-07-19 navigation audit (see §10) found no navigation path in the app links to this screen at all** — it's registered in `app/(main)/_layout.tsx` with `href: null` but nothing ever calls `router.push` to it, so fixing its data source alone wouldn't make it visible to any user. **Data-source fix is blocked on a reachability decision (new tab slot vs. a link from Home) — pending, see §3 and §10.** Don't assume either the data fix or a reachability fix is done in a future session without checking. **Note (2026-07-24): a separate "Wallpaper Hub" was built inside `daily-status.tsx` (see §23) with its own Wallpapers sub-tab — that's a different, working implementation and does not use these orphaned files at all. This paragraph still describes `wallpapers.tsx`/`wallpaper-detail.tsx` specifically, which remain unreachable orphans; don't conflate the two.**

The only screen currently pulling real `type: 'wallpaper'` Feed data from the API is **"Daily Status"** (`app/(main)/daily-status.tsx`, via `useFeed({ filters: { type: 'wallpaper' } })` → `FeedList` → `FeedCard` → `WallpaperFeedCard`).

Re-confirm this split before relying on it — it's exactly the kind of thing a future session might fix without it being recorded here.

**Title vs. caption field confusion (fixed 2026-07-18, see §6 for the fix):** the `Feed` type has two separate fields — `title` (multilingual JSON object, e.g. `{"en": "Test Ringtone"}`) and `caption` (a plain string, currently always `null` for anything created via the API or CSV ingestion). Several components were reading `caption` where they meant to resolve `title`. **Open product question, not yet decided:** should `caption` be built out as a real short-blurb/description feature, or removed from the schema entirely since nothing populates it? Don't assume either direction — ask the founder before building on top of `caption` or removing it.

## 5. Horoscope engine

Confirmed: the daily Rashifal feature is not doing real astrological generation. `src/services/horoscopeAPI.service.js` calls two free public third-party APIs (`aztro.sameerkumar.website`, then `horoscope-app-api.vercel.app`) and, if both fail, falls back to `getDefaultHoroscope()` — a hardcoded object of static per-sign strings with a `Math.random()` "lucky number." Separately, `astrology.service.js` does real western zodiac-sign calculation from date of birth, but Vedic details (moon sign, ascendant) are explicitly stubbed as placeholders in the code (`// TODO: Future enhancement - call Vedic astrology API`).

Intended direction: a custom nightly AI-generation workflow to replace the third-party-API-plus-fallback approach. Not yet built — confirm current state before assuming otherwise.

## 6. Known resolved issues (history only — don't redo these)

- **Seeder JSON bug** — fixed, committed (`bhav-bhakti-be` `15c2b0a`): a seeder crashed inserting plain strings into JSON-typed multilingual columns; fixed by wrapping values in `JSON.stringify({ en: ... })`.
- **AWS S3 → Firebase Storage migration** — committed (`bhav-bhakti-be` `67c303b`), confirmed working end-to-end 2026-07-18 (real upload → Feed → app playback). AWS package/script cleanup was *not* part of this — see §2.
- **Feed title/caption field-mismatch bug** — fixed 2026-07-18 (`bhav-bhakti-fe` `4e65b55`): several components (`RingtoneFeedCard`, `FeedCard`'s mantra render, `mantras.tsx`) read `feed.caption` (always null) instead of resolving the multilingual `feed.title` object, showing generic placeholders instead of real content. Fixed to resolve `feed.title[language] || feed.title.en`. `MantraCard.tsx` had the identical bug but is confirmed dead code (never imported anywhere). Open question, still undecided: should `caption` become a real short-blurb feature, or be removed from the schema? See §4.
- An original infrastructure audit and two 2026-07-19 reference documents (a Product Status Report `.xlsx` and a Navigation Map `.html`) exist only as founder-held files outside both repos — not in git, don't assume Claude Code can see them.

## 7. Session state — uncommitted local changes (resolved)

As of 2026-07-22, `bhav-bhakti-fe`'s `git status` was clean — the two previously-tracked uncommitted items (`package-lock.json`'s "unexplained modification," never actually root-caused but confirmed byte-identical to `HEAD`; and `src/shared/config/api.ts`'s hardcoded `10.0.2.2` override) are both resolved (the latter properly fixed in §12). Founder has HeidiSQL installed for direct DB browsing — see §8 for when to use it vs. Claude Code.

## 8. Working style / preferences

- Founder is non-technical: always explain *why*, not just *what*, when something requires a decision from them (e.g. hosting choice, auth approach, tradeoffs between doing something quickly vs. correctly).
- Flag security- and cost-relevant issues clearly and promptly (e.g. the open `POST /api/v1/feed` endpoint, the master-OTP auth bypass) — but don't block ongoing work on low-priority cleanup items (AWS leftovers, stale docs) unless asked.
- After completing meaningful milestones, remind the founder to commit to GitHub — and always confirm sensitive files (keys, credentials, `.env`, `firebase-service-account.json`) are gitignored *before* committing, not after.
- **Founder prefers checking data directly in HeidiSQL for simple lookups** — viewing a table's contents, checking whether a single row exists, eyeballing a value. Only use Claude Code for data questions that require actual code logic, multi-table joins/queries, or scripting (e.g. counting bilingual-field fill rates across several tables, cross-referencing DB rows against what the app code actually does with them). When a request comes in that's really just "what's in this table" or "does this row exist," flag that it could be checked directly in HeidiSQL instead of running it on the founder's behalf by default.

## 9. Localization / i18n — audit findings (2026-07-19)

**Language switcher is genuinely functional**, not a mock. Custom-built (no i18next), a Zustand store `src/shared/stores/i18nStore.ts` persisted to AsyncStorage, defaulting to Hindi. Supports 4 languages: English, Hindi, Gujarati, Bengali. Main switcher: Profile screen modal (`app/(main)/profile.tsx:325-353`), all 4 languages, genuinely calls `setLanguage` and persists. Secondary toggle on the Horoscope screens (`src/components/molecules/LanguageToggle.tsx`, used in `horoscope.tsx:116` and `horoscope-detail.tsx:139`) only flips English/Hindi — Gujarati/Bengali users can't reach their language from there, only from Profile.

Caveat: there are **three separate hand-rolled translation-string systems** doing overlapping jobs in incompatible formats (one confirmed dead code). Not currently broken, but confusing to extend — worth consolidating before adding more UI text.

**Quiz question/answer display and the underlying data are genuinely correct and working.** `app/(main)/mantra-quiz.tsx` resolves `questionText`/`questionSubtext`/`optionText` through the current language correctly (lines 136-144, 163). Live DB fill rates confirmed by direct query (2026-07-19): **Deities 20/20 rows fully bilingual** (`deities` table, also has Telugu/Tamil/Bengali/Gujarati beyond en/hi); **Quiz + Questions + Options 100% bilingual** (1 quiz, 5 questions, 29 options, every row has both `en` and `hi`). This is the one part of localization that's fully real end-to-end.

**Categories: 49/55 bilingual.** All 55 have bilingual `display_name`. 6 rows are missing a Hindi `description` — the original seed-data categories (ganesh, shiva, durga, lakshmi, hanuman, universal, ids 1-6) from `src/database/seeders/20260315000001-seed-mantra-categories.js`, which only ever wrote an `en` key for `description`. The other 49 (from the CSV-driven seeder) are fully bilingual.

**Feed content (actual app content — wallpapers/ringtones/mantras) is essentially empty — a content-loading problem, not a translation problem.** The live `feeds` table has exactly 1 row (the test ringtone from §2), and its title is English-only. The import script (`scripts/import-feeds-from-csv.js`) already correctly handles `title_hi`/`caption_hi` CSV columns and the source CSV (`Content Upload Sheet - Feed.csv`) already has Hindi text filled in for sampled rows — the script just hasn't been run against this database yet. Whatever content tool gets built (see §3) should require both languages up front to avoid reproducing the categories gap at scale. **Note: this is now stale for feed volume — real content has since been imported (§25); the categories-gap risk this paragraph describes is worth re-checking against actual imported content rather than assuming it played out this way.**

**Gujarati/Bengali: offered in the language picker but only ~25% translated.** `src/locales/gu.json`/`bn.json` are ~66 lines each vs. `en.json`'s 281 / `hi.json`'s 246 — missing whole sections (wallpaperDetail, spiritual, auth, premium, horoscope, zodiacSigns, rashiNames, elements, errors, success). Falls back silently to English for missing keys. All quiz content (questions/options) is typed and stored as `{en, hi}` only — Gujarati/Bengali quiz-takers always see English questions regardless of their language setting, since there's no `gu`/`bn` data to select even though the resolution code is language-aware. **Decision pending, not made:** hide Gujarati/Bengali from the picker until translation is complete, or commit to finishing them. Don't assume either direction.

**Per-user language preference: backend-ready, frontend never wires it up.** `UserProfile.model.js:59` has a `language` ENUM supporting 10 languages (`en, hi, mr, gu, ta, te, bn, kn, ml, pa` — more than the app exposes), and `profile.controller.js` already reads/writes it on `GET`/`PUT /v1/user/profile`. But the frontend never calls this — there's no real "Edit Profile" screen (`app/(main)/profile.tsx:43-44`'s menu item is just an `Alert.alert('coming soon')`), and nothing anywhere sends a `language` field in a profile update. Language selection today is **100% phone-local** (AsyncStorage only) — resets to the default on reinstall or a new device, with no way for the backend to know or use a user's preference even though the schema already supports it.

**Quiz recommendation — the actual payoff of the quiz — is completely unbuilt on the frontend, and half-built on the backend.** `mantra-quiz.tsx` never calls the existing result-fetching functions (`useSubmitAnswer`, `useQuizResults` — both defined in `src/features/quiz/hooks/useQuiz.ts` but never imported anywhere). Finishing the quiz just fires a hardcoded-English `Alert.alert('Quiz Completed!', ...)` and redirects to the generic Mantras tab — no recommendation, in any language, is ever shown to the user. Even if the frontend called it, the backend logic (`src/services/quiz.service.js`) isn't ready: `personalizedMessage` is genuinely bilingual (lines 236-279), but `practiceSchedule` (lines 213-234) is hardcoded English with no `hi` counterpart at all, and gets interpolated verbatim into the Hindi message too — so even a working Hindi recommendation would contain an English phrase like "5-10 minutes." Worse, `recommendedMantras` — the field that should hold the actual recommended mantra `Feed`(s) — is initialized empty (line 137) and never populated anywhere in the file, despite `Feed` being imported at the top. The feature's namesake output doesn't exist yet.

**Recurring bug, not caught by the 2026-07-18 fix (see §6):** `app/(main)/audio-player.tsx` (the mantra playback screen) has its own local `getLocalizedText` helper (lines 148-162) that **ignores the current app language entirely** — it always tries `en` first, then `hi`, then whatever's first in the object, regardless of the user's language setting. It also still reads `feedData.caption` (lines 183, 196) instead of resolving `feed.title[language]`, the same caption/title bug §6 documents as fixed elsewhere — this screen just wasn't included in that fix. `app/(main)/index.tsx:282` and `app/(main)/search-results.tsx:83` propagate the same bug by passing `feed.caption || 'Sacred Mantra'` (a hardcoded English fallback literal, not run through any translation function) into this screen's navigation params. Net effect: a Hindi-language user opening a mantra to actually play it will see English description text and a generic "Sacred Mantra" title instead of the real, correctly-stored bilingual content — on what's arguably the app's core feature. **Not yet reconfirmed fixed or unfixed since 2026-07-19 — re-check before assuming this is still exactly true, since `audio-player.tsx` has been heavily rewritten since (§13/§16/§25/§26).**

## 10. Navigation map — screen reachability audit (2026-07-19)

**18 screen files total** (`app/index.tsx`, 2 in `app/(auth)/`, 15 in `app/(main)/` — not counting the 3 `_layout.tsx` route-group files, which aren't screens). **5 have bottom-tab slots**: Home (`index.tsx`), Mantra (`mantras.tsx`), Ringtone (`ringtones.tsx`), Status (`daily-status.tsx`), Rashifal (`horoscope.tsx`) — all defined in `app/(main)/_layout.tsx`. The **"Status" tab routes to `daily-status.tsx`, not `wallpapers.tsx`** — Daily Status is the one screen actually pulling real `type: 'wallpaper'` Feed data (see §4). **Note: since this audit, "Ringtone" was relabeled "Audio" (§21) and "Status" was relabeled "Wallpapers" (§23) — both route names are unchanged, only labels/content changed; the underlying reachability facts below still hold for the specific orphaned files named.**

The complete navigation graph (every `router.push`/`router.replace` call site in the app — no `<Link>` components are used anywhere, and there's no deep-link/`Linking` handling beyond opening OS settings) is documented in a standalone **Navigation Map (`.html`)** reference document kept outside the repo (see §6) — not duplicated in full here to avoid this file drifting out of sync with that document.

**4 of 18 screens (≈22%) are currently unreachable by any navigation path in the app**, confirmed by grepping every navigation call site, not just the tab config:
- **`wallpapers.tsx`** — registered (`href: null`) in `_layout.tsx` but zero navigation calls anywhere target it. See §3/§4 for why this blocks the Wallpapers data-source fix.
- **`wallpaper-detail.tsx`** — only reachable *from* `wallpapers.tsx` (`wallpapers.tsx:77-78`). Since `wallpapers.tsx` itself is unreachable, this screen is unreachable in practice too, even though the code path technically exists.
- **`spiritual.tsx`** — a fully-built screen with its own mock data (`src/data/mockSpiritual.ts`), registered in `_layout.tsx`, but nothing anywhere navigates to it.
- **`zodiac-selection.tsx`** — registered in `_layout.tsx`, but superseded: the Rashifal tab's zodiac grid (`horoscope.tsx:31-35`) navigates straight to `horoscope-detail.tsx` instead, bypassing this screen entirely. Looks like a leftover from before that grid existed.

**`spiritual.tsx` and `zodiac-selection.tsx` are candidates for either linking up or deleting — undecided.** Don't assume either direction in a future session; confirm with the founder. Same caution applies to `wallpapers.tsx`/`wallpaper-detail.tsx`, though those are already tracked as a known gap via §3/§4.

## 11. 2026-07-20 session — Ringtones playback/caching, play count tracking, Trending Now planning

**Ringtones playback fixes** — done, tested, pushed: single-active-sound exclusivity, stop-on-tab-navigation, stop-on-app-backgrounding, and position-reset-on-stop, all built into `ringtones.tsx`/`RingtoneFeedCard.tsx` (later superseded by the global playback coordinator, §17). **Local caching** — done, tested (airplane mode): `ensureLocalFile()` gives every ringtone one stable cache filename shared by play/download/set-ringtone, and fixed a real extension-detection bug (was reading the URL's `?alt=media` query string). **"Set as Ringtone"** confirmed fully working via a real dev build (required moving off Expo Go, and fixing `JAVA_HOME`/`ANDROID_HOME` on this machine). **Play count tracking** — done, pushed: `plays_count` column + `POST /feed/:feedId/play`, built to power a "Trending Now" section designed this session (ranked by `playsCount`, 7-day window, top 5) — endpoint spec agreed (`GET /feed/trending?...`), later built (§22). **Figma MCP** — connection added (user-scoped), OAuth login not yet completed at the time. **Standing process adopted:** an "Audio Content Pre-Work Scoping Checklist" (playback exclusivity/caching/platform/product questions) to apply before building any new audio content type — still worth applying to future audio work. **Deferred, low-priority, not fixed:** no dedup on `MediaLibrary.saveToLibraryAsync`; no concurrency guard on `ensureLocalFile()`. **Bug found this session** (Home's ringtone cards play simultaneously with the Ringtones tab) — later fixed by the global playback coordinator, §17.

## 12. 2026-07-21 session — Network fix, visual/design audit, Aarti/Bhajan foundation, Audio hub decided

**Network fix** — done, committed (`bhav-bhakti-fe` `9473442`): `api.ts`'s hardcoded `10.0.2.2` dev override replaced with automatic host detection via `Constants.expoConfig?.hostUri`.

**Visual/design audit** (8 screens) — findings logged, mostly still unactioned as of this writing: missing brand assets app-wide (icon, splash, quick-link icons, card thumbnails — one consolidated future task); ≥5 inconsistent header treatments; color system drifted heavily from `DESIGN_SYSTEM.md` (only 2 colors documented, 8+ in real use — zodiac colors likely intentional, rest is drift); inconsistent screen-title/empty-state copy; a real "Ganesh Mantras" duplicate-category bug (root-caused §13). **Open product decisions, still pending:** (a) should "Status"/"Wallpapers" merge or stay distinct — later resolved differently via the Wallpaper Hub, §23; (b) Rashifal's date-browsing arrows — build out or leftover UI; (c) keep or replace zodiac emoji icons. **Standing rule adopted:** colors/fonts/icons always come from `DESIGN_SYSTEM.md`; component-level patterns get derived from existing coded screens, not further Figma queries (free-tier call budget).

**Major architecture decision, since built (§21):** the Ringtone tab becomes an "Audio" hub with horizontal sub-tabs (Ringtones/Aartis/Bhajans, expandable). Mantras and Stories stay separate, full bottom-nav tabs. Aartis/Bhajans need Spotify-style persistent playback (mini-player, backgrounding, lock-screen) — confirmed as new infrastructure, not an adaptation of `RingtoneFeedCard`/`audio-player.tsx`.

**Foundational schema work — done, committed** (`bhav-bhakti-be` `5b24338`; `bhav-bhakti-fe` `84aac2d`, `9473442`): `'aarti'`/`'bhajan'` added to `Feed`/`Category` type ENUMs; new `is_repeatable` boolean on `feeds`. Six separate hardcoded type-whitelist call sites across the backend had to be found and updated by hand — root cause: no shared `FEED_TYPES` constant exists anywhere, so every new type still means touching every whitelist individually (worth fixing when Stories is added). Frontend types/logic updated to match; repeat-counter logic decoupled from `type === 'mantra'` to `feed.isRepeatable`.

**Designed this session, built later:** the global playback coordinator (§17), the audio-session `setAudioModeAsync` conflict fix (§17), and the Audio hub's sub-tab UI (§21, built from scratch — no sub-tab pattern/library existed anywhere in the app).

## 13. 2026-07-21 session (continued) — Audio player redesign, seek bar diagnosed, category duplicate confirmed, Home tap/startup jitter root-caused

**Audio player redesign** — done, committed (`bhav-bhakti-fe` `b784490`): `audio-player.tsx` restructured from a scrolling 4-block layout into a fixed 3-region layout (header strip, flexible visual area, compact control bar), no `ScrollView`. Chant counter and content description moved into two `@gorhom/bottom-sheet` overlays (`CounterSheet`/`InfoSheet`). Dependencies restored (`react-native-gesture-handler`, `reanimated`, `worklets`, `@gorhom/bottom-sheet` — previously stripped in an undocumented April cleanup commit, safe to reverse). Screen's data function renamed `getMantraData`→`getContentData`, a deliberate move toward being content-type-agnostic (shared player for Mantra now, Aarti/Bhajan later).

**Important, still relevant:** this player is meant to be a GLOBAL/persistent player (audio survives navigating away), unlike Ringtones' intentionally screen-scoped behavior. At this point in the project it still incorrectly stopped on back-navigation — the real fix was the global playback coordinator, built §17.

**Seek bar — diagnosed, unfixed as of this writing** (still unfixed as of the last check, §15/§24). Two compounding causes: the thumb is a plain non-interactive `View` with only a tap `onPress`, no real drag gesture; and position updates are coarse (expo-av's default 500ms interval, never shortened). Needs a real `Gesture.Pan()` rebuild.

**"Ganesh Mantras" duplicate category — confirmed root-caused:** category ids 1 (`ganesh`, original seeder) and 7 (`ganesha`, later CSV seeder) are two real, never-reconciled rows for the same deity — causes both the visible UI duplicate and silent empty-result filtering depending which spelling a tap uses.

**Home screen tap — root cause confirmed, fix deliberately parked (superseded by later fixes, see §25):** `handleFeedPress` only had a navigation branch for repeatable audio content, so wallpapers/ringtones/aarti/bhajan cards on Home silently no-opped. Founder parked a full fix pending a larger Home-layout decision (type-specific sections vs. one mixed feed) — **not yet made**, don't patch piecemeal.

**Startup jitter root cause identified, not fixed:** the Verify OTP screen briefly renders on top of Phone Login during the auth transition.

**Live test content created** (`bhav-bhakti-be` `e4d126f`): Feed IDs 3 (Test Mantra) and 4 (Durga Wallpaper) uploaded via a new reusable script, `scripts/test-upload-mantra-wallpaper.js`.

## 14. 2026-07-22 session — react-native-track-player evaluated and removed

**Decision: use `expo-audio`, not `react-native-track-player`, for persistent/background audio.** `react-native-track-player` (free v4.1.2) was installed, natively configured, tested, then fully removed after hitting a genuine compile-time incompatibility with this project's toolchain (Expo SDK 54 + New Architecture) — confirmed via an actual Gradle build failure (Kotlin nullability mismatch), not just a warning. The only version that compiles against current toolchains, v5/`@rntp/player`, is commercially licensed (€99/mo+) — not viable on current budget. Removal was verified fully clean (native config reverted, rebuild succeeded, `git status` clean). Re-attempt only if budget changes or a free-tier fix ships. **Trade-off to remember for Stories:** `expo-audio` has no built-in queue/playlist concept — multi-episode "queue + autoplay-next" will need to be hand-built on top of it.

## 15. 2026-07-23 session — Animation fix, autoPlay-on-tap fix, Ringtones migrated to expo-audio, USB dev-build crash fix

Four commits (`bhav-bhakti-fe`: `1e4d09c`, `3a71b73`, `5f49f91`, `9d2e663`). **Animation freeze fix** — `pulseAnim`/`rotateAnim`/`waveAnims` (audio-player's wave visualizer, later removed entirely in §26) were plain `const`s instead of `useRef`, so re-renders silently orphaned the running animation after ~1s; fixed via `useRef`. **AutoPlay-on-tap fix** — added `autoPlay` param + a guarded mount effect so tapping "play" on a mantra card actually starts playback immediately (previously needed a second manual tap); this specific mechanism was later found to have a latching bug for a second mantra in the same session, fixed in §25. **Ringtones migrated `expo-av`→`expo-audio`** — done, verified on a real phone: `RingtoneFeedCard.tsx` rewritten around `useAudioPlayer`/`useAudioPlayerStatus`; all previously-documented ringtone behavior (exclusivity, stop-on-nav/backgrounding, caching) reverified working. Known accepted trade-off: ~500ms visual lag on tap-to-seek (not patched — reintroducing manual position state would undo the reactive-hook benefit). **USB dev-build routine established** (needed again after any USB reconnect, not just fresh rebuilds): `adb reverse tcp:3000 tcp:3000` + `adb reverse tcp:8081 tcp:8081`, confirmed via `adb reverse --list`. **Two non-bugs confirmed intentional:** Hindi-as-default-language is a deliberate hardcode (no device-locale detection exists); the Feed 3/4-vs-1/2 Hindi-title inconsistency is an authoring gap in test scripts, not a translation bug.

## 16. 2026-07-23 session (continued) — FeedMedia cleanup, content-rendering map, audio-player migrated to expo-audio, coordinator confirmed as next milestone

**`FeedMedia.tsx`'s dead `expo-av` audio code removed** (`39480aa`) — confirmed unreachable; all real audio playback now lives in exactly two places: `RingtoneFeedCard.tsx` (ephemeral) and `audio-player.tsx` (persistent).

**Content-rendering architecture map, still relevant, not yet actioned:** Ringtone and Wallpaper are each correctly one reused component everywhere. **Mantra is fragmented** — `FeedCard.tsx`'s `renderMantraCard()` (Home/Search), Mantra Explorer's own separate inline `renderMantraCard` (different denser layout), and a third, confirmed-dead `MantraCard.tsx`. Consolidating the two live ones into one `MantraFeedCard` (mirroring `WallpaperFeedCard`'s later variant pattern) is flagged as valuable, still not done — may want a `variant` prop rather than a forced single design, since Explorer's dense row may be an intentional distinct "browse list" treatment.

**`audio-player.tsx` migrated `expo-av`→`expo-audio`** (`ddd33c2`), emulator-verified. Old state/retry scaffolding replaced with reactive `status` reads. **Real crash fixed:** `player.playbackRate = X` is a getter-only property at runtime despite TS types — must go through `player.setPlaybackRate()`. **Flagged then, still worth checking if not yet fixed:** `player.loop`/`player.volume`/`player.shouldCorrectPitch` use the same unsafe direct-assignment pattern — the manual repeat/loop button doing nothing, and the counter sheet's "More" button UX sequencing bug, are both still-open bugs logged this session, suspected to share this root cause; confirm current status before assuming either is fixed.

**Structural finding (superseded — see §17):** this session incorrectly concluded persistence-across-navigation was blocked by `useReleasingSharedObject`'s auto-release; §17 later found the real explanation (Tabs don't unmount screens) and corrected this.

**Polish backlog, still not built:** universal like/share/download buttons across all audio content types (should live in the shared player shell); a first-time-use discovery tooltip for the counter icon.

**Root cause identified for the Home/Ringtones simultaneous-playback bug:** `FeedList.tsx` has no playback-exclusivity props — fixed by the coordinator, §17.

## 17. 2026-07-23 session (continued) — global playback coordinator built and shipped

**Global playback coordinator — done, committed (`3de37e5`), thoroughly tested on-device.** `src/store/playbackStore.ts` (Zustand, deliberately not `persist`ed — it holds live imperative closures bound to a mounted player instance). Two independent slots, not one shared "now playing": `ephemeral` (ringtones) and `persistent` (mantra today, aarti/bhajan/stories later). Hand-off rules: same-mode hand-off (ringtone→different ringtone, or persistent→different persistent feedId) fully stops the outgoing item; cross-mode hand-off is asymmetric — a ringtone preempting persistent content **pauses** it (stays resumable in the mini-player), persistent content preempting a ringtone **fully stops** it (no paused UI to preserve). This two-slot split was a genuine mid-build correction after real device testing showed the original single-slot design made the mini-player vanish on any ringtone tap, instead of showing paused-and-resumable — the intended Spotify/YouTube-Music-like behavior.

**New component `MiniPlayer`** (`src/components/molecules/MiniPlayer/`): rendered as a sibling of `<Tabs>`, reads only the `persistent` slot (a ringtone can never appear here, enforced at the selector level), hidden while already on `/audio-player`. Pause/resume plus an explicit stop/dismiss (✕); tapping the body navigates to the full player.

**Root cause fix:** `RingtoneFeedCard.tsx` now self-registers with the store directly (`registerPlaybackStart`/`clearNowPlaying`) instead of relying on parent-supplied props — this is what gives Home/Search Results real cross-screen exclusivity and stop-on-blur for free, with zero changes to `FeedList.tsx`.

**Important corrected understanding (retroactively fixes §16's structural finding):** `audio-player.tsx`'s persistence-across-navigation was never actually broken — `(main)` is a `Tabs` group and React Navigation doesn't unmount inactive tab screens by default. What the coordinator actually added was *deliberate management* on top of that pre-existing accidental persistence: cross-screen exclusivity, mini-player visibility, and a real explicit stop.

All 7 designed test scenarios (rapid hand-offs, mini-player stop, pause-not-clear on preemption, resume-from-correct-position, mini-player hidden/reappearing correctly around the full player, Home's ringtones stopping on any tab switch) confirmed working on a real device.

**Known, accepted, minor risk, not yet hit:** a `RingtoneFeedCard` could theoretically unmount while still registered in the `ephemeral` slot, leaving a stale `activeControls` reference — a defensive try/catch was recommended, not yet verified needed in practice.

## 18. 2026-07-23 session (continued) — lock-screen/notification controls built, a crash fixed, zero error boundaries found

**Lock-screen/notification controls — done, committed, verified on a real device.** `audio-player.tsx` activates `expo-audio`'s native lock-screen/notification controls for persistent content while playing — confirmed a genuine, complete implementation on both platforms (real `MPNowPlayingInfoCenter`/`MPRemoteCommandCenter` on iOS via source inspection; real `MediaSessionService` + foreground service on Android, working end-to-end on a real device).

**Real crash found and fixed:** tapping the lock-screen notification to return to the app could crash (`ForegroundServiceStartNotAllowedException`) because `setActiveForLockScreen` was called unconditionally, including during Android 12+'s restricted background→foreground window. Fixed by gating on a confirmed `AppState === 'active'` check plus a retry-on-foreground-transition effect and a defensive try/catch.

**Important standing finding: this app has zero error boundaries anywhere** (confirmed via full-repo grep) — any unhandled crash on any screen produces a blank white screen with no fallback. Not fixed, logged as a general resilience gap independent of any one feature.

**Retracted false alarm:** "loop/counter stops when minimized" was a testing artifact of Expo Go (which lacks full native background-audio support), not a real bug — retested clean on the real dev-build app. Lesson: always confirm which environment (Expo Go vs. real dev-build) a background/native test came from.

**Deliberate scope decisions, still current:** `interruptionMode` stays `duckOthers` (not `doNotMix`) — audio ducks rather than pauses for all interruptions including real calls, accepted since distinguishing interruption types needs custom native code; no deep-link-to-specific-screen from the notification (the `MiniPlayer` already covers the practical need); iOS entirely out of scope (no native `ios/` project exists).

**Cosmetic bug, not fixed:** lock-screen title/metadata doesn't match what's shown in-app (`artist` mapped to deity name) — worth reconsidering.

## 19. 2026-07-23 session (continued) — lock-screen confirmed on a real phone, WiFi issue narrowed, emulator regression root-caused (fixed next session, see §20)

Lock-screen controls confirmed working hands-on by the founder on their own phone. **Physical-device WiFi connectivity issue narrowed, not resolved:** raw network reachability was confirmed fine (Metro's `/status` endpoint reachable from the phone's browser) — the actual failure is specific to how the Expo dev-client itself connects to Metro, not network/firewall. USB remains the reliable workaround. **Emulator regression found and root-caused:** the `EXPO_PUBLIC_DEV_API_HOST` override (added for physical-device WiFi testing) had no device-type condition, so it broke the emulator's Home feed too. Fix (gate the override behind `expo-device`'s `Device.isDevice`) was written but needed a native rebuild to test — confirmed working the next session, §20.

## 20. 2026-07-24 session — isDevice fix confirmed, recurring WiFi IP-reassignment documented, an unidentified network error logged

**§19's `isDevice` fix confirmed working, committed (`5e9e0b8`)** — physical device's Home feed loads correctly again.

**Standing operational fact, recurs regularly:** home WiFi periodically reassigns the laptop a new local IP, silently breaking physical-device testing until `bhav-bhakti-fe/.env`'s `EXPO_PUBLIC_DEV_API_HOST` is manually updated to match. Check via `ipconfig`'s "Wireless LAN adapter Wi-Fi" entry specifically (other entries can show misleading addresses). A full Metro restart is required after any `.env` change (`EXPO_PUBLIC_*` vars inline at bundle time). Expect to repeat this periodically for as long as the manual-IP-override approach is in use.

**Low-priority, unidentified, still open:** a repeating `Network request failed` error appears in logs on some endpoint — confirmed not the Home feed request, not caused by polling. Source not identified; revisit if it starts causing a visible symptom.

## 21. 2026-07-24 session (continued) — Audio hub restructure shipped, a real crash found and fixed

**Audio hub — done, committed (`01bbd0b`), tested on a real device.** Bottom nav's "Ringtone" tab relabeled "Audio," hosting three horizontal sub-tabs (Ringtones/Aartis/Bhajans) via a plain tap-only pill row (no swipe — no sub-tab UI pattern/library existed anywhere in the app). Ringtones' list UI extracted into `RingtonesTabContent`; Aarti/Bhajan were "Coming Soon" placeholders until built in §27. Route name kept as `ringtones` (only the label/icon changed) so no existing nav call sites broke. Deep-link-to-sub-tab works via a `subTab` param synced through a plain (not once-only) effect — deliberately re-syncable since Tabs don't unmount, so a repeated tap on the same Home quick-link still switches sub-tabs correctly.

**Design decision, still true:** inactive sub-tabs fully unmount rather than staying hidden — accepted since Aarti/Bhajan started as empty placeholders; revisit once they have real content and this becomes user-visible thrashing (they now have real content per §27 — worth re-checking whether this still feels fine).

**Real crash found and fixed** (`5fad2c4`), confirmed working on a real device: switching sub-tabs while a ringtone played crashed with "Cannot use shared object that was already released." Root cause: `useAudioPlayer`'s internal cleanup releases the native player *before* the component's own custom cleanup effects run (React runs effect cleanups in declaration order) — any cleanup code touching the player after release throws. This had existed since the `expo-audio` migration but never fired because Tabs don't unmount screens during normal use; the hub's conditional sub-tab render was the first code path to actually unmount `RingtoneFeedCard` instances on demand. Fixed with try/catch at three call sites (no synchronous "is released" check exists on `expo-audio`'s player type, so try/catch is the only available guard) — **this same crash class recurred at least twice more in later sessions (§24, §25) in different files/call sites; treat any new player-touching cleanup code as needing the same guard by default.**

## 22. 2026-07-24 session (continued) — shared deity/trending filter shipped for Ringtones, three trending bugs found and fixed

**Deity/Trending filter — done, committed, tested end-to-end with real data (Ringtones first; later extended to Aarti/Bhajan, §27).** A shared filter row sits above the sub-tab pill row in the Audio hub, state held at the **hub level** (not per-sub-tab) so a selection persists across sub-tab switches. Chips: Trending (default) → Liked (reserved, not wired up) → 8 fixed-order primary deities → More (remaining deities + "Others," sorted by `sortOrder`). Selecting a chip stops any playing ringtone via the same stop mechanism as sub-tab switching. **Visibility is declared per sub-tab via a required `showDeityFilter: boolean` field on each `SUB_TABS` registry entry** — TypeScript makes it mandatory so a future sub-tab can't be added without deciding this, closing off the "forgot to update the Nth spot" bug class this project hit before (§12).

**New component `DeityFilterRow`** (`src/components/molecules/DeityFilterRow/`) — generic (`deities`/`selected`/`onSelect` only), designed to drop into future sub-tabs unchanged (confirmed reused as-is for Wallpaper Hub, §23, and Aarti/Bhajan, §27).

**Backend, all committed:** "Others" is a real Deity row (id 21, `sortOrder: 9999`), not a null-`deityId` special case — every chip queries identically. `categoryId`/`deityId` are now genuinely validated on create/update/getFeeds (previously passed through only because express-validator ignores unlisted fields). `deityService.ts`/`useDeities()` added, mirroring the category pattern. Thumbnails use each deity's existing emoji `icon` + gradient `colors` fields — no new image assets.

**Three real, pre-existing bugs in `GET /feed/trending` found and fixed** (endpoint had never been exercised by the frontend before this session): the controller silently ignored its own `type` query param; the response was missing a `pagination` object entirely (caused a real crash on the frontend, fixed via `findAndCountAll`); trending paginated by `likesCount` at the DB level then re-sorted an already-limited page by a different computed `engagementScore` in JS — fixed by moving the real engagement-score formula into the DB-level `ORDER BY` (root cause of the original workaround: Sequelize's subquery wrapping means only camelCase aliases, not raw snake_case columns, are available in the outer query scope).

**`useRingtones()` reworked** to accept a `filter` param and branch between trending/deity-filtered queries — previously took no parameters and never sent `type` to the server at all (a pre-existing, unrelated gap).

**Next, extended later:** the deity-filter pattern was built anticipating Aarti/Bhajan and Wallpaper reuse — both happened (§23, §27). Minor logged UX item, still open: the collapsed "More" chip doesn't show which deity is active when selected from inside the expansion panel.

## 23. 2026-07-24 session (continued) — Wallpaper Hub built end-to-end, mirroring the Audio hub pattern

**Wallpaper Hub — done, committed, verified end-to-end with real data and on-device testing.** Bottom nav's "Status" tab relabeled "Wallpapers," `daily-status.tsx` rebuilt into a hub with three sub-tabs — Status (default) / Thought for the Day / Wallpapers — same minimal-churn approach as the Audio hub (route name kept, only label changed).

**Content model, three buckets through one endpoint (`GET /feed`), no new routes:** Status = `?type=wallpaper` (superset, no occasion filter); Wallpapers = `?type=wallpaper&statusOccasion=none` (general-purpose only, `'none'` as a sentinel for `IS NULL`, since an absent param already means "don't filter"); Thought for the Day = `?type=thought` (new Feed type, visually identical to wallpapers — the "thought" text is baked into the image itself).

**Schema:** new nullable `statusOccasion` ENUM (`good_morning`/`good_evening`/`good_night`/`festive`); `'thought'` added to `Feed`/`Category` type ENUMs following the aarti/bhajan whitelist-update pattern. Two real gaps found and fixed beyond what was asked: the service-layer field whitelist didn't include `statusOccasion` (would have silently never persisted despite passing validation); `GET /feed/trending` didn't support `statusOccasion` either (would have silently ranked across all wallpapers regardless of the Wallpapers sub-tab's own bucket definition).

**`WallpaperFeedCard.tsx` made variant-aware** (`variant?: 'default' | 'grid-tile'`, defaults `'default'`) rather than forked — pure additions, zero changes to existing default-mode code (confirmed via diff). Grid-tile: no title/text, single image, 9:16 `aspectRatio` with `resizeMode="contain"`, Like/Share/Download as one bottom overlay bar. Real bug found and fixed: a lone item in an incomplete 2-column `FlatList` row expanded to fill the full width (modern RN doesn't insert filler cells) — fixed with a calculated `GRID_TILE_WIDTH` constant. **`DeityFilterRow` reused unchanged** from the Audio hub; `showDeityFilter: false` on Thought for the Day is the first real exercise of that per-sub-tab toggle for a sub-tab with no deity concept.

**IMPORTANT, still not actioned:** the `variant` split was deliberately temporary scaffolding — founder's stated intent is to eventually unify to one card style everywhere (Home included), matching how `RingtoneFeedCard` already works, retiring default mode. Don't assume this has happened without checking.

**Still open, logged, not urgent:** real share-destination functionality (WhatsApp Status/Instagram Story deep-linking) is parked as out of MVP scope for all card types; Firebase Analytics integration is a flagged roadmap item, not started.

## 24. 2026-07-27 session — Cache-Control metadata pass for CSV-imported media, persistent player local caching ported from Ringtones, CDN deferred

**Cache-Control metadata on upload — done, committed, verified** (`bhav-bhakti-be`): `scripts/import-feeds-from-csv.js` now sets `Cache-Control: public, max-age=31536000` on every media/thumbnail file it processes, as a separate, non-blocking pass that runs only after the DB import's transaction has already committed — a Storage failure here can't roll back the actual content import. Verified via a real dry run (permission, merge-not-overwrite, and untouched file content all confirmed against a live file) before the full build. Only resolves genuine `firebasestorage.googleapis.com` URLs matching the configured bucket; legacy CloudFront/S3 URLs and bucket mismatches are skipped and logged, not guessed at. Runs in concurrency-capped batches of 10.

**Scope note:** this only benefits the CSV import pipeline (the founder's real workflow, §3) — it does not apply to the in-app upload flow (`uploadService.js`), deliberately untouched since it isn't in real use.

**Real CDN (Cloud CDN + load balancer in front of Firebase Storage) — investigated, explicitly deferred, not built.** Cache-Control metadata alone gives no genuine edge caching for direct `firebasestorage.googleapis.com` URLs (that's Firebase's own serving layer, not Cloud CDN) — real edge caching needs a separate Load Balancer + Cloud CDN project (reserved IP, backend bucket config, domain/cert, ongoing cost). Correctly out of scope for MVP; revisit once real volume/billing data justifies it, likely paired with the still-open Railway hosting migration (§2).

**Persistent player local caching — done, committed, verified on a real device** (`bhav-bhakti-fe`): `audio-player.tsx` now has the same download-once, play-from-disk caching Ringtones already had, ported onto the shared persistent player — applies automatically to any content type playing through this screen (Mantra now, Aarti/Bhajan/Stories later) since the caching function only needs `feedId` + `audioUrl`. **Real improvement over the original Ringtones pattern:** cache files are keyed by `feedId`, not sanitized title text (Ringtones' original approach has a latent collision risk between similarly-titled feeds — not backported there, still an asymmetry). **Real crash risk found and fixed:** converting `togglePlayback` to `async` reintroduced the "shared object already released" crash class if the screen unmounts mid-download — fixed with the same `isMountedRef` + try/catch pattern from §21. **Two additional hardening fixes, beyond what was asked:** a failed/partial download now cleans up its partial file (also not yet backported to Ringtones); the 10s load timeout now starts only once a resolved URI is handed to the native player, not from the top of the function.

**Not yet addressed, explicitly deferred:** no concurrency guard on near-simultaneous calls into the cache function for the same file — accepted as low-risk today (one user, one tap at a time), flagged to revisit once queue/playlist logic (a real second concurrent caller, prefetch) is built.

**Two new bugs found, not fixed this session:** the seek bar's position display appears to update only every ~5s (possibly, not confirmed, related to the known drag-gesture issue, §13); re-opening a mantra after dismissing it via the mini-player doesn't autoplay, unlike the first-ever open (root-caused and fixed next session, §25 — it was the `autoPlayTriggeredRef` latching bug).

## 25. 2026-07-27 session (continued) — First real bulk CSV import validated, a significant cross-content playback bug fixed, caching sequencing fixed, a native connection-pool bug found (fixed next day, §26)

**This section's work was held back uncommitted for a full day before going to GitHub** — the connection-pool bug below was unresolved and the founder wanted it fixed first; §26 resolved it, and everything here is now committed and pushed (see §26's closing note for exact SHAs).

**First real bulk content import — confirmed clean.** 29 test items (all content types) imported via `scripts/import-feeds-from-csv.js` — the first real test under actual content volume. Two importer fixes built and verified this session: a `diety id` (space, not underscore) column-matching bug in `cleanFeedData()`, and `statusOccasion`/`thought`/`aarti`/`bhajan` support added to `transformFeedData()`'s type mapping.

**Real, significant bug found and fixed: mantra/aarti/bhajan cards all played the SAME audio regardless of which card was tapped.** Root cause: `togglePlayback`'s gate (`if (status.isLoaded)`) only checked whether *anything* was loaded, never whether the loaded content matched the *newly-requested* `feedId` — since `audio-player` is a `Tabs.Screen` reused across every mantra tap, once any mantra had ever loaded, every later tap on a different one just resumed/paused the first. Dormant since the original migration (§16); only manifested once two real different mantra rows existed to tap in the same session. **Fixed:** added `loadedFeedIdRef`, gating resume/pause on `status.isLoaded && loadedFeedIdRef.current === feedId` — a mismatch now falls through to a fresh load.

**Two compounding bugs, same root-cause class, fixed in the same pass:** `autoPlayTriggeredRef` was a single boolean that permanently latched after the first-ever mantra, silently blocking autoPlay for every mantra tapped after it — replaced with a per-feedId ref (this is what closed out §24's "doesn't autoplay on reopen" bug). Stale `feedData` could briefly leak the *previous* item's title/type/`isRepeatable` into the coordinator registration during the async refetch window — fixed by requiring `feedData.id.toString() === feedId` before trusting it anywhere, falling back to route params (which every real nav call site already passes accurately) in the meantime.

**Real bug found and fixed: Home's "Play now" silently did nothing for CSV-imported content.** `handleFeedPress` gated navigation on `feed.isRepeatable`, but the CSV importer never set it, so every imported mantra defaulted to `false` while the button rendered as if tappable. Fixed in two parts: frontend navigation now depends only on whether the feed has audio media (matching Mantra Explorer's existing, correct behavior) — `isRepeatable` now correctly only controls the in-player counter UI, not navigation; backend importer now defaults `isRepeatable: true` specifically when the *mapped* type is `mantra` (checked against the mapped type so `chalisa`→`mantra` is swept in too — aarti/bhajan keep the schema's `false` default, since they're sung-through, not chant-repeated). The 4 pre-existing CSV-imported mantra rows were updated directly via a scoped `UPDATE`.

**Caching sequencing regression found and fixed: fresh plays were waiting for the ENTIRE file to download before starting** — a genuine regression introduced by §24's own caching work earlier the same day. Fixed by restructuring `togglePlayback` to play immediately from the remote URL on a cache miss and download to local cache in the background, non-blocking (`getCachedLocalUri` for the fast local check, `downloadToCacheInBackground` built on `DownloadResumable` for the actual download). Background downloads were also made genuinely cancellable, hooked into every real stop path (pause, coordinator stop/pause, preemption, backgrounding, unmount). **The identical restructure was backported to `RingtoneFeedCard.tsx`**, which had the same bug, just invisible there since ringtone files are small.

**Real race condition found and fixed (verified via an isolated timing test):** reopening the same content shortly after cancelling its background download could collide with that cancellation's own still-in-flight cleanup, producing real `Download failed`/`Network request failed` symptoms that self-resolved after waiting. Fixed in `audio-player.tsx` only (not yet backported to `RingtoneFeedCard.tsx` — a known, logged asymmetry) via a per-feedId `inFlightCancellations` map that both cache-read and cache-write paths check first, plus a dedup guard for repeated rapid re-attempts. Confirmed zero added delay in the common case.

**Separate crash found and fixed: `clearLockScreenControls` could throw "shared object already released" during rapid feedId switching** — two unprotected call sites (the coordinator's stop/pause closures, and the unmount cleanup) now wrapped in try/catch (deliberately not AppState-gated, unlike the `setActiveForLockScreen` fix in §18 — this is a shared-object lifecycle issue, not a foreground-service timing restriction).

**MAJOR ARCHITECTURAL FINDING, root cause identified via direct native source inspection — fixed the next day, §26.** Opening fresh content while a *different* item sits paused in the mini-player could show a false "network request failed"-style error that self-resolved after dismissal. Root cause, traced in `expo-audio`'s bundled native Kotlin source: a single shared `OkHttpClient` (no custom `Dispatcher`) is wired into every `AudioPlayer` app-wide, inheriting OkHttp's default `maxRequestsPerHost = 5`. A paused track's connection commonly stays warm for instant resume rather than closing immediately; tearing it down via `Call.cancel()` isn't instantaneous (worse on larger files) and can still occupy one of the 5 slots the shared client needs for a fresh track. Confirmed precisely (not just inferred) that the fresh track's own 10s client-side load-timeout — not any real permanent failure — is what actually fires and displays the alert. **Important reframing:** this bug is not a consequence of this session's caching work at all — it's a pre-existing architectural property of the persistent player (present since the coordinator, §17) purely from the mini-player's "keep paused content warm" design plus `expo-audio`'s unconfigured shared HTTP client, only now surfaced because this was the first session with real, varied, larger-file content to actually test "pause one thing, play something else."

**Fix chosen (built next day, §26): raise the shared client's `maxRequestsPerHost`.** Other options considered: explicitly detach A's media source before B loads; extend B's own timeout tolerance instead of fixing the contention.

**Also flagged, genuinely still open, not resolved by the connection-pool fix:** whether the full caching/cancellation complexity built across this session is proportionate given zero real users yet, versus reverting to simple direct streaming until real usage data justifies it.

## 26. 2026-07-28 session — Connection-pool fix built and confirmed, real root cause of icon/seek-bar lag found (a dead decorative animation, not expo-audio), .env IP update

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across both repos for a single shared project narrative; `bhav-bhakti-be`'s own `git status` is unaffected by anything in this section.

**Connection-pool fix (§25's chosen option) — built, confirmed working on a real device, committed and pushed (`2836b6a`).** Patched `expo-audio`'s native `AudioModule.kt` directly (`node_modules/expo-audio/android/.../AudioModule.kt`) to give its shared `OkHttpClient` a custom `Dispatcher` with `maxRequestsPerHost` raised from OkHttp's default of 5 to 20 — directly targeting the root cause §25 diagnosed. Since this edits `node_modules`, `patch-package` was added as a devDependency with a `postinstall` script, and the fix captured as `patches/expo-audio+1.1.1.patch` — without this the edit would silently revert on the next `npm install`. Confirmed on-device: the false "network request failed" error on opening fresh content while something sits paused is now dramatically reduced. One remaining, understood exception (not a bug): one large 50MB+ mantra file still takes noticeably longer to become ready, but no longer shows a false error — genuine load time now, not contention.

**Investigation into the separately-reported play/pause icon lag and jumpy seek bar.** Initial theory (two `expo-audio` native code paths giving `isLoaded` genuinely different definitions, causing flicker) was explicitly disproven by real captured Metro logs (a clean single transition, no flicker) and correctly abandoned rather than force-fit. **Actual root cause: `audio-player.tsx`'s wave bar visualizer** — 20 bars animating `height` (can't use the native driver, so every frame ran on the JS thread/bridge), running continuously for the entire duration of playback, directly competing with the JS thread needed for play/pause and seek-bar updates. Confirmed via grep to be purely decorative/fake (`Math.random()`-driven, zero connection to real audio amplitude — no `useAudioSampleListener` wiring exists anywhere). A dead one-time debug effect from an earlier investigation (§15) was found alongside it.

**Fixed — the wave bar visualizer was removed entirely, not optimized.** Deliberate call: it was fake, actively cost responsiveness on the two controls users actually complained about, and making it performant (native-driver-compatible `scaleY` transform) was judged disproportionate for a purely cosmetic feature with zero real users yet. `pulseAnim` (the play-button pulse, already native-driver, already cheap) was kept as the sole "is playing" cue. Layout self-adapted cleanly with no manual fix needed.

**Confirmed on-device: both play/pause response and seek-bar smoothness significantly improved.** Wave-bar removal is believed the dominant fix, since it was a continuous full-playback-duration cost directly in the path of the complained-about controls.

**Standing lesson worth remembering:** seemingly-related symptoms (loading lag, icon lag, seek-bar lag) had two entirely unrelated root causes (a native connection-pool limit vs. a decorative JS-thread animation) — investigate fresh if anything resurfaces rather than assuming a connection to either fix.

**Minor operational note:** `.env`'s `EXPO_PUBLIC_DEV_API_HOST` updated to `192.168.1.7` (new WiFi network) — same recurring pattern as §20, needs a full Metro restart after any change.

**Committed and pushed, both repos — §25's and this session's work went together, once §25's connection-pool blocker was actually fixed.** `bhav-bhakti-fe` (`ce70c81..4389e55`): `2836b6a` (connection-pool patch), `dc2e477` (`audio-player.tsx` — §25's playback fixes + this session's wave-bar removal, one commit since the diff hunks matter more for review than history), `b6ce6b9` (Home's "Play now" fix), `4389e55` (Ringtones caching backport). `bhav-bhakti-be` (`85231df..4d6701b`): `4d6701b` (CSV importer fixes). **Deliberately left uncommitted:** two `Content Upload Sheet` CSV files in `bhav-bhakti-be` — real content data whose diff couldn't be confirmed as intentional from the repo alone, held for the founder's own review. `.env` remains gitignored, never committed.

## 27. 2026-07-28 session (continued) — Aarti/Bhajan Audio hub tabs built and shipped, generalized back-navigation fix, Search Results isRepeatable bug fixed

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across both repos for a single shared project narrative; `bhav-bhakti-be`'s own `git status` is unaffected by anything in this section.

**Aarti/Bhajan tabs built — done, committed, tested on real device (`727298c`).** The Audio hub's "Coming Soon" placeholders (§21) are replaced with real, functional tabs. New shared `useAudioFeed(type, filter)` hook (`src/features/feed/hooks/useAudioFeed.ts`) mirrors `useRingtones()`'s trending/deity branching and pagination, parameterized by `type: 'aarti' | 'bhajan'`. New `AartiTabContent`/`BhajanTabContent` (thin `FlatList` wrappers) and a new shared `AudioContentCard` that **navigates** to `audio-player.tsx` on tap rather than embedding its own inline player — deliberately not modeled on `RingtoneFeedCard`'s self-contained pattern, since Aarti/Bhajan need persistent, mini-player-capable playback (the same shared player already used by Mantras) rather than ephemeral list-scoped playback. Confirmed working: both tabs show real content, tapping plays correctly through the shared player, and the deity filter (§22) works on both.

**Generalized back-navigation fix — done, committed, tested on real device (same commit).** Fixes the long-standing bug where tapping back from `audio-player.tsx` always returned to Home regardless of actual entry point (most visible from Mantra Explorer). Root cause not fully diagnosed (the Tabs navigator's implicit back-history was misbehaving in some undiagnosed way). Rather than depend on that, every real entry point (Home, Mantra Explorer, Search Results, `AudioContentCard`) now explicitly passes `returnTo`/`returnParams` when navigating to the player; the player's back button uses these when present, falling back to `router.back()` otherwise. Confirmed working across all tested entry points.

**Search Results audio-navigation fix — done, committed, tested (`8d0b227`).** Same `isRepeatable`-gating bug Home had (fixed §25) was also in `search-results.tsx` — Aarti/Bhajan search results were tappable but silently did nothing. Fixed identically: navigation now depends on the feed having audio media, not `isRepeatable`.

**Real finding, not yet fixed: search only matches `Feed.caption` (and `location`/`tags`), never the multilingual `title` field.** It "works" today only because the CSV importer happens to copy `title_en` into `caption` as a side effect — not a deliberate design. Feeds 1-4 (pre-CSV-importer hand-scripted content) have `caption: null` and are **not searchable at all today**. No relevance ranking, no multilingual matching. Logged as its own future task — search deserves a deliberate rebuild, not a patch.

**Real finding, not yet scoped: what the player displays (title, description, deity, artwork) per content type depends on whichever `Feed`/`FeedMedia` fields happen to be filled at content-creation time** — this mapping accumulated feature-by-feature across §13/§16/§25's work rather than being deliberately designed. Logged as a future task: audit which fields the player needs per content type and confirm the CSV pipeline reliably captures them.

**Next planned work, not started:** a deliberate UI/design pass across Ringtone/Mantra/Aarti/Bhajan cards — founder is doing manual competitive research (Zedge, Sri Mandir, YouTube Music) first, to work from concrete references.

**Commits, `bhav-bhakti-fe` only** (`bhav-bhakti-be` untouched): `727298c` (Aarti/Bhajan tabs + back-nav fix, 12 files), `8d0b227` (Search Results fix, 1 file). Both pushed. Also this session: `.env`'s `EXPO_PUBLIC_DEV_API_HOST` updated to `192.168.0.105` (new network, same recurring §20 pattern).

## 28. 2026-07-29 session — Aarti/Bhajan auto-loop fix, full 8-phase queue feature built, two lock-screen items deferred

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across both repos for a single shared project narrative; `bhav-bhakti-be`'s own `git status` is unaffected by anything in this section.

**Auto-loop/restart bugs fixed, committed (`570a3ee`), confirmed working on-device.** `shouldAutoLoop` now gates on `isRepeatable` (mantra unaffected) and `player.pause()` runs before `seekTo(0)` on natural finish, fixing an ExoPlayer auto-resume glitch.

**All 8 phases of the Aarti/Bhajan queue feature complete, committed (`2a489ac`, `dfafb62`, `2f4afdf`), tested on-device.** Dead UI removed, shuffle-ready queue store, Previous/Next, auto-advance-on-finish, Like/Share, and an "Up Next" queue sheet with tap-to-jump. Mantra untouched throughout, by construction. **Two lock-screen items deferred, not fixed:** (1) skip-next/previous on the lock screen/notification shade still show the old +10s/-10s seek behavior — plan already scoped (reuse the existing seek-button `SessionCommand` pattern with real `ICON_NEXT`/`ICON_PREVIOUS` icons), genuinely simpler than (2) and safe to pick up first; (2) lock-screen title/artwork mismatch (shows the audio file's embedded metadata instead of our app's data) — a fix attempt was investigated and reverted after hitting real complexity in the fragile `AudioControlsService`/`MediaSession` area, and later testing suggests this may just be a loading delay rather than a real bug — needs a calm, patient re-test before deciding if any fix is even needed.

## 29. 2026-07-31 session — Home feed viewport-autoplay: scoped, not built

**Scoped only, not built this session.** Home feed viewport-autoplay (whichever card is 60%+ visible plays, one at a time, universal `AutoplayFeedCard` for all types) was fully planned as a 6-phase build. Built the very next session — see §30. Full original scoping detail archived in `CLAUDE_ARCHIVE_2026-07-29_full-history.md`.

## 30. 2026-08-01 session — Home feed viewport-autoplay Phases 1-4 built and shipped

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across both repos for a single shared project narrative; `bhav-bhakti-be`'s own `git status` is unaffected by anything in this section.

**Phases 1-4 done, committed, tested on-device** (`fb8b518`, `39e5fc9`). Viewability infra: 60% visibility threshold, one elected card via a 200ms debounce plus a 500ms floor on starting a new player (connection-pool sensitivity, §25/§26) — scoped to Home only via the opt-in `enableViewportAutoplay` flag, `false`/unattached everywhere else.

**Routing expanded to "any feed with media,"** not an audio-only check — video isn't its own `Feed.type` (it's a `FeedMedia.type` value), so this already future-proofs video content with zero further routing changes needed.

**Dual card sizing, both self-measured** (`getItemLayout` dropped for this path only): audio fixed at ~88% of usable viewport height; wallpaper/video sized to match audio cards' width (no longer full-bleed) for visual alignment and a proportionally shorter card/better peek. This also fixed a real pagination-blank-scroll bug — a stale fixed `getItemLayout` estimate (600px/item) badly mismatched the new cards' real rendered height and broke infinite scroll.

**`AutoplayFeedCard` built, replacing the stub:** isolated per-card player (zero `playbackStore` involvement, by design — overlap with the real/persistent player is an accepted trade-off, not a bug), 30s-or-natural-length cap for audio, unbounded loop for video, a manual play/pause overlay, centered 70%-width CTA pills (Listen / Set as Ringtone / Set as Wallpaper) with consistent styling, ringtone-type content sets the ringtone directly rather than opening the full player, a clearly-marked **TEMPORARY** paywall stub (`isPremiumUser = false`) gating all three pill actions, an evenly-spaced Like/Share/Views footer, and a shared `formatCount` utility (K/M abbreviation).

**Confirmed unaffected:** Mantra untouched; Ringtones tab, Mantra Explorer, Wallpaper Hub, and Search Results all confirmed unaffected via `enableViewportAutoplay` staying `false` everywhere except Home.

**Still pending, not yet built:** Phase 6 (on-device hardening pass) — **correction, see §31:** Phase 5 (muted-by-default video + persistent unmute) was actually already built in this same commit set (`soundPreferenceStore` + `AutoplayFeedCard`'s `isMuted`/mute-button wiring), just never flagged as such; it was never actually pending. Real paywall wiring is a separate future task, blocked on an actual payment/entitlement backend that doesn't exist yet.

## 31. 2026-08-01 session (continued) — Phase 5 confirmed already built, app-backgrounding autoplay bug fixed, Wallpaper Hub snap-to-card feed scoped

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across both repos for a single shared project narrative; `bhav-bhakti-be`'s own `git status` is unaffected by anything in this section.

**Phase 5 (muted-by-default video + persistent unmute) confirmed already built alongside Phase 4, not a separate pending phase — §30's "still pending" note was stale.** `soundPreferenceStore.ts` (persisted, `isVideoMuted` default `true`) and `AutoplayFeedCard.tsx`'s `isMuted`/mute-button wiring were both already committed in `fb8b518`/`39e5fc9`, just never flagged as fulfilling Phase 5. Only Phase 6 (on-device hardening) remains from the original plan.

**Real bug found and fixed, committed and pushed (`bhav-bhakti-fe` `a21460b`): leaving Home via in-app navigation correctly stopped autoplay (`useFocusEffect`), but minimizing/backgrounding the app did not.** Fixed by adding an `AppState` listener (mirroring `RingtoneFeedCard.tsx`'s existing pattern) and folding it into the same `isEffectivelyActive` flag that already gates the audio effect, the 30s cap, and video's `shouldPlay` — audio and video both now stop on backgrounding through the same existing code, no separate stop path added.

**Scoped, not yet built: a TikTok/Reels-style snap-to-card feed for the Wallpaper Hub's Status sub-tab** — visual-only (wallpaper/video, never audio), reusing `AutoplayFeedCard` as-is. Key decision: uncropped 9:16 content takes priority over true edge-to-edge sizing — cards are centered/margined to match Home's audio-card width, not full-bleed, though the snap mechanism itself (`pagingEnabled`) still feels clean/edge-to-edge. Snap-scroll area height is calculated dynamically from whatever space remains below the hub's header (title + deity filter row + sub-tab pills), never hardcoded, so it adapts automatically if the header is redesigned later. Changing the deity filter mid-browse resets scroll to the first card. Not started — pick up whenever ready.

## 32. 2026-08-02 session — Status sub-tab Reels-style feed: first attempt reverted, rebuild scoped

**Frontend-only, nothing committed this session.** A FlatList-based snap-scroll Reels feed was built, then fully reverted — FlatList can't guarantee zero adjacent-item bleed no matter how it's sized, a real architectural limitation confirmed via on-device testing (two genuine sizing bugs were found/fixed along the way but didn't solve the core issue). A `react-native-pager-view` rebuild was scoped as the next attempt. **Superseded by §33: the pager-view rebuild was never pursued — the founder chose the auto-looping video grid instead, a deliberate abandonment, not a deferral.** `daily-status.tsx`/`StatusTabContent.tsx` were left back in their original working state. Full original detail (including the two sizing bugs) archived in `CLAUDE_ARCHIVE_2026-07-29_full-history.md`.

## 33. 2026-08-03 session — Wallpaper Hub video support + Viewing Window feature shipped; pager-view rebuild abandoned

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across all three copies for a single shared project narrative.

**Wallpaper Hub video support + new "Viewing Window" feature — DONE, COMMITTED (`c691eda`, `65be4d7`), tested on-device.** Video support added to `WallpaperFeedCard`'s grid (Status/Wallpapers/Thought sub-tabs) — loops silently, muted unconditionally (hardcoded `true`, independent of the shared `soundPreferenceStore`, deliberately not following Home's mute-preference behavior). New Viewing Window feature: tapping a grid tile opens a centered 80%×80% modal (NOT a bottom sheet — deliberately switched from `@gorhom/bottom-sheet` to a plain RN `Modal`, since a floating centered box isn't that library's use case), showing the image/video WITH sound, plus Like/Share/Download reusing the grid's existing handlers via a new shared `useWallpaperActions` hook. Premium-gated via the existing `isPremiumUser` stub pattern — currently hardcoded `true` for testing, will stay that way until the real paywall is built.

**Three real bugs found and fixed during testing:**
1. **Like icon inside the window didn't update in real time** — root cause was a captured data snapshot at tap-time rather than a live lookup; fixed by storing only the `feedId` and deriving current data from the live feed array on every render.
2. **Backgrounding didn't stop playback** — two layered issues: the `AppState` subscription was conditionally gated and never established in time (fixed: subscribe unconditionally on mount), and even once detection worked, nothing was actually calling pause on the video player (fixed: an explicit effect calling `videoRef.pauseAsync()` when `isEffectivelyActive` becomes false).
3. **Download reported false success and silently overwrote files** — root cause was filename collisions (built purely from content IDs, no uniqueness); fixed by appending a timestamp, plus cleaning up the redundant temp file after `MediaLibrary` copies it to the gallery (was leaking storage silently). The same filename/cleanup fix was applied to two other pre-existing call sites with the identical bug (`AutoplayFeedCard`'s Set-as-Wallpaper, `FeedCard`'s generic download) — all three now use a new shared `getMediaFileExtension` utility, replacing a hardcoded `.jpg` that was corrupting downloaded videos into unplayable image-labeled files. Also added a loading spinner to all download buttons for tap feedback, previously silent/no-feedback during the download.

**PAGER-VIEW REELS-STYLE STATUS FEED — ABANDONED, not just deferred.** After two full sessions on the FlatList snap-scroll approach (§32) hit a fundamental architectural limitation (FlatList can't guarantee zero adjacent-item bleed, confirmed via real device testing across multiple fix attempts), the founder made a deliberate product call: the auto-looping video grid (this session's work) is the actual solution going forward, not `react-native-pager-view`. The pager-view investigation/plan from §32 is kept for reference only — not being pursued. `AutoplayFeedCard.tsx` still carries a few unused leftover props (`fixedTotalHeight`, `showTypeLabel`, `showCtaPill`) from that abandoned attempt, sitting harmlessly uncommitted — safe to discard whenever convenient, not currently causing any issue.

## 34. 2026-08-04 session — Real content sharing built (react-native-share)

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across all three copies for a single shared project narrative.

**Real content sharing — DONE, COMMITTED (`0355978`, `51a6a0d`), tested on-device (wallpaper/video confirmed; audio branch built but untested, blocked on no thumbnail content existing yet, not a code gap).** Replaced React Native's built-in `Share` (no real file attachment, text-only) with `react-native-share`, since it reliably combines a real file + caption in one action on Android — confirmed via native source (a known, unfixable iOS+WhatsApp limitation exists at the OS level, moot since no iOS build exists yet).

**New shared `shareContent(feed)` utility** (a plain function, not a React hook, since it's called from many unrelated components) handles: downloading the real file with correct MIME type for wallpaper/video, thumbnail-only sharing for audio (never the audio file itself, by design), graceful text-only fallback when no thumbnail exists, timestamped filenames (reusing the collision-avoidance fix from earlier), an in-flight guard against double-taps, and incrementing the share count — all confirmed working. Loading spinner added to the Share button, using an `onSharePresenting` callback since `Share.open()`'s promise doesn't resolve until the OS share sheet is dismissed, not when it opens.

**Also cleaned up:** discarded the leftover pager-view scaffolding props (`fixedTotalHeight`/`showTypeLabel`/`showCtaPill`) from `AutoplayFeedCard.tsx`, confirmed dead via a repo-wide grep before removing — the abandoned pager-view plan itself remains documented in §33 for reference, only the unused code was discarded.

**Two deliberately deferred, confirmed-doable-later items:** (1) content-type-specific caption wording (e.g. per deity/aarti vs. generic). (2) Paywalling the Share button via the existing `isPremiumUser` stub pattern.

## 35. 2026-08-04 session (continued) — Lock-screen skip-button fix: root cause of zero on-device effect found, config fix applied, not yet rebuilt/verified

**Root cause found, fix applied but unverified this session:** the skip-button fix showed zero on-device effect because `expo-audio` links a stale prebuilt AAR by default instead of compiling from the patched source — confirmed via direct dex-file decompilation of the installed APK, not just a successful build. This cast doubt on whether the earlier connection-pool fix (§26) was ever in a real tested build either. Config fix applied (`expo.autolinking.android.buildFromSource`) but not yet rebuilt/verified as of this session. **Superseded by §36: the fix was confirmed working on a real device next session, and the connection-pool fix was re-confirmed genuinely working for the first time with real evidence.** Full original detail archived in `CLAUDE_ARCHIVE_2026-07-29_full-history.md`.

## 36. 2026-08-05 session — Skip-button fix DONE, confirmed on a real device; buildFromSource root cause confirmed via dex decompilation

**Skip-button fix — DONE, COMMITTED, TESTED ON REAL DEVICE (both notification shade and lock screen).** Root cause of skip buttons never working: `expo-audio` ships as a pre-built binary; Gradle links that prebuilt AAR by default instead of compiling from patched source — meaning ALL prior native patches (including the connection-pool fix) may never have been in a real, tested build. Fixed via `expo.autolinking.android.buildFromSource: ["expo-audio"]` in `package.json` (the official escape hatch), forcing Gradle to compile from source. Confirmed via direct dex-file decompilation of the installed APK, not just "BUILD SUCCESSFUL" — real proof, not assumption. Once building from source genuinely worked, the connection-pool fix (from weeks ago) was RE-CONFIRMED working for the first time with real evidence — fast, error-free track switching now genuinely observed.

**Skip buttons:** first attempt (adding NEW skip commands alongside old seek commands) caused a real regression — competing buttons, inconsistent behavior across notification shade vs. lock screen (different surfaces showed different combinations of old/new buttons). A deep architectural investigation into player mount/remount-on-navigate was pursued but did NOT cleanly solve the problem and was fully reverted (confirmed via `git diff` matching the last real commit exactly). Final, correct fix: simple rewiring — the existing seek commands (`ACTION_SEEK_FORWARD`/`ACTION_SEEK_BACKWARD`) now directly trigger real skip-next/previous, with icons swapped to real `ICON_NEXT`/`ICON_PREVIOUS` on the lock screen specifically (notification shade icons already looked correct). No new commands added — deliberately avoiding the earlier competing-buttons mistake. Seek-by-10-seconds behavior deliberately removed entirely, by founder's choice — not needed.

## 37. 2026-08-05 session (continued) — Lock-screen/notification skip-vanish bug: two real root causes fixed and confirmed on-device; a new, separate metadata-race finding logged, not yet investigated

**Lock-screen/notification skip-vanish bug — TWO real, distinct root causes found and fixed, tested on-device.** (1) The playback coordinator's same-mode hand-off logic couldn't distinguish "same player instance, new content" from "genuinely different player" — added an optional `instanceId` (React `useId()`, stable per mount) to fix the false-positive `stop()`/`clearLockScreenControls()` firing on every skip. (2) `activateLockScreenControls()` always did a full native session teardown+rebuild on every skip, even without any bug — that's just how it was written. Found and switched to an already-existing, already-native-exposed lightweight update path (`updateLockScreenMetadata`) that updates the session in place instead — no native rebuild needed to use it, since it was already fully wired to JS and simply never called.

**Combined, the notification shade now updates seamlessly on skip — CONFIRMED WORKING.** Lock screen improved but still has some rough edges — deliberately not polished further, needs more real-world testing before deciding if further work is warranted. Also added: a 30-minute auto-dismiss for a paused lock-screen entry (native-only — clears via the existing `clearLockScreenControls()`, deliberately leaves the mini-player/in-app resumable state untouched).

**NEW FINDING, NOT YET INVESTIGATED:** confirmed a genuine timing race for lock-screen metadata — the app's correct data (CSV title, no thumbnail) displays first on skip, then gets overwritten roughly a second later by the audio file's own embedded metadata (the original Firebase filename, embedded artist/thumbnail). Two sources writing to the same display, and whichever writes second wins regardless of correctness. Needs investigation into where the embedded file metadata is actually being read from and why it's allowed to override already-set app data. Not chased this session.

## 38. 2026-08-06 session — Lock-screen metadata fix: PARTIAL, title confirmed working, artwork gap documented, not fixed

**Metadata mismatch fix (§37's finding) — PARTIAL, committed as-is, not silently presented as done.** Two-part fix built: explicit `MediaItem` metadata (title/artist/artwork) declared at load time from the already-available `contentData`, plus disabling ExoPlayer's `TRACK_TYPE_METADATA` track selection so nothing should compete with it. **Title fix confirmed working via real on-device testing** — though not yet proven against a file with genuinely competing embedded title data; the current test files may simply lack one, so this isn't fully conclusive yet. **Artwork fix confirmed present and compiled but NOT effective.** Root cause: `TRACK_TYPE_METADATA` disables the wrong mechanism — embedded cover art (ID3 `APIC` frames) on local/progressive audio files rides directly on the audio track's own `Format.metadata`, not a separate disableable metadata track, so the disable call never touches it. Committed as-is with this documented honestly rather than presented as fixed — the title win is real progress, the artwork gap is inert (no crash/regression), just not yet closed.

**NEXT REAL TEST, not yet run:** once a genuine thumbnail is uploaded to any track, check whether the app's data or the file's embedded picture wins — that's the honest signal needed before any further work here. If title also gets overridden once tested against a file with real embedded title data, the same root issue applies there too, meaning the title "fix" was untested luck, not a genuine confirmation.

## 39. 2026-08-11 session — App branding: icon, splash screen, display name, package identifier

**App branding — DONE, COMMITTED (`c9d4632`, `c9071b8`).** New app icon and splash screen logo (proper design, replacing placeholders); app display name corrected to "Bhav Bhakti" (was showing as "bhavbhakti", no space). Android/iOS package identifier changed from `com.vivekpandit.bhavbhakti` (previous developer's name) to `com.orivori.bhavbhakti` — confirmed safe (no Play Store listing, no signing keys, no Firebase app registration tied to the old identifier).

**Real gotcha worth remembering:** icon artwork needed real margin/padding within the 1024x1024 canvas (not edge-to-edge) to avoid Android's adaptive-icon shape-masking cropping into the logo — took several iterations to get right.

**Reminder:** any dev-client build under the old package name won't auto-update — must be manually uninstalled from test devices to avoid a duplicate, orphaned icon.

## 40. 2026-08-12 session — Rashifal MVP core built: real AI content, birthdate-collection flow, three real bugs found and fixed

**Rashifal MVP core — DONE, COMMITTED, TESTED ON-DEVICE.** Real AI-generated daily horoscope content (Gemini free tier, 24 isolated calls/day = 12 signs × 2 languages, findOrCreate-safe). Old dead reactive fallback chain (Aztro/horoscope-app-api) removed — missing content now fails visibly instead of silently locking in placeholder text forever.

**New personal flow:** Home's "Today's Horoscope" card collects a birthdate once via a native date picker, calculates+saves zodiac sign, then always routes straight to it — never paywalled. Separate 12-sign browsing grid (quick-link pill) is unchanged, will get the paywall in Phase 4 (not built yet).

**THREE REAL BUGS found in previously-untested code, all fixed:** (1) profile API calls used a URL that never existed (`/user/profile` instead of `/profile`). (2) 8 horoscope routes had `validate` passed uninvoked — silently hung forever, no error, no response, first real request ever exposed it. (3) **SIGNIFICANT:** `new Date().toISOString().split("T")[0]` silently returns YESTERDAY's date for any IST user/process between midnight-5:30am — was live in 6 places (the generation script + 5 content-serving paths), meaning real users could silently see stale horoscope content every single night during that window, with a correct-looking date heading masking mismatched content underneath. Fixed via one shared, local-date utility (mirrored in both repos) — the same pattern already used correctly in `BirthdateModal.tsx`.

**NOT YET FIXED, logged, low priority:** same date bug in `getWeeklyHoroscope()` (likely dead/unused — not part of MVP scope) and the original seeder script (one-time, already run, low risk).

**Phase 4 (paywall on the grid) and Phase 5 (final verification) still pending.**

## 41. 2026-08-12 session (continued) — Trending 7-day window removed, previously undocumented

**DONE, COMMITTED (`a4d16e5`), tested.** `getTrendingFeeds` previously excluded any content older than 7 days for all types except mantras, which already had an explicit carve-out — silently returning empty/sparse trending results for other content types with low, spread-out volume. The `days`/`createdAt` filter is now dropped entirely, so trending ranks across all active content regardless of age, matching mantra's existing behavior. Pre-existing fix from before this session's Rashifal work, unrelated to it — only now being logged here since it was never documented at the time.

## 42. 2026-08-12 session (continued) — Rashifal Phase 4 shipped, premium status stub consolidated

**This entire session's work is frontend-only (`bhav-bhakti-fe`) — no backend changes.** Logged here too since this doc is kept mirrored across all three copies for a single shared project narrative.

**Rashifal Phase 4 — DONE, COMMITTED (`d791560`), TESTED ON-DEVICE.** Paywall gate added to the 12-sign browsing grid (reuses shared premium status, see below); Home's "Today's Horoscope" personal path remains never-paywalled. Date-navigation pill (prev/next arrows) removed entirely from `horoscope-detail.tsx`, matching the today-only MVP decision — full cleanup including now-dead `formatDisplayDate()`, its styles, and the unused `Haptics` import. **All 5 Rashifal phases now complete:** real Gemini-generated content, correct dates, personal + browsing paths, paywall, on-device verified end to end. Full UI/visual design pass remains explicitly deferred, same bucket as the Ringtone/Mantra/Aarti/Bhajan card redesign (§27).

**Premium status consolidated — DONE, COMMITTED (`6b4681d`).** Found 3 separate, disagreeing `isPremiumUser` stubs (`AutoplayFeedCard.tsx`, `horoscope.tsx`, `useViewingWindow.ts` — one was `true`, two `false`) built independently across different sessions, unaware a real, more complete mechanism (`usePremiumStore`, with a full paywall modal already wired to `profile.tsx`) already existed unused. All 3 now read from that shared store. One real, deliberate behavior change: the Wallpaper Hub's Viewing Window gate went from open to closed, matching the correct intended default. New single `DEV_OVERRIDE_IS_PREMIUM` seam inside `premiumStore.ts` for local testing — structurally only one flip point now, not three.

**STILL NEEDED, logged for later:** per-user premium status (not just an app-wide toggle) requires real user accounts (Firebase Auth) and a backend `isPremium` field — neither exists yet, and connects directly to the still-pending real payment/paywall work.

## 43. 2026-08-12 session (continued), RESOLVED 2026-08-13 — Firebase Phone Auth Phases 1-4 built, real numbers confirmed working via reCAPTCHA fallback

**Phases 1-4 code COMPLETE, COMMITTED, PARTIALLY VERIFIED.** Backend (`bhav-bhakti-be`: `2437f22` token verification, `16d1e60` find-or-create + session issuance) and frontend (`bhav-bhakti-fe`: `f3b92e8` RN Firebase install/config plugin, `c6258bc` real `signInWithPhoneNumber`/`confirm` flow with a module-level confirmation-object bridge) both built and working — confirmed via Firebase test phone numbers logging in successfully end-to-end. Old `/send-otp`/`/verify-otp` endpoints and all OTPless code deliberately left untouched — Phase 5's job, not this one.

**REAL PHONE NUMBERS NOT YET WORKING — root cause fully diagnosed with direct evidence (an actual reproducing API call), not guessed:**
1. **FIXED:** the Android API key had an "Android apps" application restriction (added during earlier security hardening after GitHub flagged it in `google-services.json`, see below) — this conflicts with Firebase phone-auth's reCAPTCHA fallback, a documented, known Firebase architectural issue (reCAPTCHA runs in a browser context, incompatible with an Android-app-restricted key; confirmed via two independent GitHub issue reports, not assumed). Changed the restriction to "None," kept API restrictions in place for real protection.
2. **STILL BLOCKING:** reCAPTCHA itself has never been set up in this Firebase project — Firebase Console's own UI confirms: "You must first set up reCAPTCHA to create site keys, and then return here to configure this feature." Almost certainly required specifically because real-number testing uses a locally-built, USB-sideloaded dev-client APK — Play Integrity (the primary, preferred verification path) is a poor match for sideloaded builds by design, so the app is correctly falling back to reCAPTCHA, which itself needs real setup.

**NEXT SESSION:** set up reCAPTCHA (site key creation in Google Cloud, linking to Firebase) before real-number sign-in can be fully tested. Test phone numbers already work correctly and can be used for continued development in the meantime.

**RESOLVED 2026-08-13 — both open items above fully answered with researched, sourced evidence, real-number sign-in confirmed working end-to-end:**
1. **Play Integrity is unavailable for a USB-sideloaded dev-client build by design — Google's own documented behavior, not a config gap.** A sideloaded APK gets an `UNRECOGNIZED`/`Unevaluated` App Recognition verdict because Play Integrity attests apps Google Play itself distributed; no Firebase- or GCP-side configuration changes that. The real fix is distributing dev-client builds via a **Play Console internal testing track** instead of raw USB `adb install` — a genuine dev-workflow change (testers pull the build via the Play Store app rather than a direct sideload). **Deliberately deferred, not pursued now.** The reCAPTCHA fallback — including its slowness and the wrong-domain (`firebaseapp.com`) SMS text — will resolve automatically once real Play Store distribution begins; nothing further to fix in the meantime.
2. **The "you must first set up reCAPTCHA" Console message (point 2 above) was a red herring.** That message is for **reCAPTCHA Enterprise**, a separate, optional Google Cloud product — not the mechanism actually in use. Firebase's Android phone-auth SDK falls back to its own auto-provisioned, zero-setup **reCAPTCHA v2** tied to the project's `firebaseapp.com` domain (this is also why the SMS shows that domain in place of the real app name — same root cause). It started working correctly the moment the API-key restriction was fixed (point 1 above); reCAPTCHA Enterprise setup was never actually required.

**DECISION:** accept the current reCAPTCHA-based flow for all remaining development. No further action needed on this item until closer to real Play Store release, at which point Play Integrity should start working automatically with no additional Firebase-side changes.

**Also this session, unrelated:** `google-services.json` was briefly committed (`f3b92e8`) before GitHub flagged its embedded Android API key; investigated and confirmed low real risk (the same key that ships in every built APK regardless, not a privileged credential) — untracked going forward (`f659837`), left in git history by deliberate choice, not pursued further given the API-restriction fix above was the real mitigation.

## 45. 2026-08-13 session — Firebase Phone Auth Phase 5 shipped: fake OTPless bypass fully removed, all 5 phases complete

**Firebase Phone Auth — FULLY COMPLETE, ALL 5 PHASES DONE, COMMITTED, TESTED ON REAL DEVICE.** Real Firebase phone verification is now the ONLY auth path — the fake OTPless bypass (master OTP `123456`, `TEST_NUMBERS` allowlist, `SEND_OTP_ENABLED`) is fully **REMOVED**, not just disabled — the actual security hole this app has carried since the start is closed. Old `/send-otp`/`/verify-otp`/`/resend-otp` routes deleted entirely (not deprecated stubs), `otpless.service.js` deleted (confirmed dead, no rollback plan).

**Confirmed via real testing:** existing pre-Firebase users correctly log into their SAME account (no duplicates); wrong codes handled cleanly; resend works.

**Real-number sign-in currently routes through Firebase's reCAPTCHA fallback** (slower, and SMS shows `firebaseapp.com` instead of the app name) — root cause fully diagnosed as an EXPECTED limitation of USB-sideloaded dev builds specifically (Play Integrity requires Play Store distribution to recognize an app). Both symptoms will resolve automatically once real Play Store distribution begins.

**Deliberately not pursuing SMS auto-fill** — investigated, would require either the SMS Retriever API (real but non-trivial future work) or direct `READ_SMS` permission (confirmed a real Play Store rejection risk for an app with no legitimate default-SMS-handler use case) — ruled out entirely.

## 44. 2026-08-12 session (continued) — Dependency drift cleanup, app icon resized

**Dependency drift cleanup — DONE, COMMITTED (`9c65f5a`), TESTED.** Ran `npx expo install --check`/`--fix`, updating 6 packages to Expo-recommended versions (`expo`, `expo-file-system`, `expo-font`, `expo-linking`, `expo-router`, `react-native-svg`) — all patch-level updates, no breaking changes. Confirmed `expo-audio`'s native patch (the connection-pool fix, §26) reapplied cleanly during install.

**App icon also resized** in this same session (bigger, still within Android's adaptive-icon safe zone established in §39) — both changes rebuilt and verified together on-device, no regressions found.
